{
  "name": "node_notion_api",
  "version": "1.0.0",
  "main": "./lib/index.js",
  "license": "MIT",
  "dependencies": {
    "@notionhq/client": "^2.0.0",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "tslint": "^6.1.3",
    "twilio": "^3.78.0",
    "typescript": "^4.7.4"
  },
  "files": [
    "./bin/*",
    "./lib/*"
  ],
  "typings": "./lib/index.d.ts",
  "scripts": {
    "install": "npm install",
    "build": "tsc",
    "start": "npm run build && npm run test && node lib/main.js",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "prepublish": "npm run install && npm run build",
    "test": "jest"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.18.6",
    "@types/express": "^4.17.13",
    "@types/jest": "^28.1.4",
    "@types/node": "^18.0.3",
    "ts-jest": "^28.0.5",
    "jest": "^28.1.2"
  },
  "engines": {
    "node": "18.x"
  }
}
